major: packages/core/src/spike-parity.test.ts:388 The binary classifier examines only quoted values that already contain the contiguous text `harness.js`, so a computed path such as `path.join(spike, 'bin', `harness${'.js'}`)` produces neither `reachesBinary` nor a problem and can be silently classified as library-only. This violates AC-2’s explicit requirement that an indirect or computed binary specifier fail rather than default. Make the classifier refuse unresolved path construction used by spawn/exec sites (or use syntax-aware analysis), and add a demonstrated failing case where `harness.js` is assembled from multiple values.

nit: packages/core/src/test-command.test.ts:510 The new job register says `port-freeze-sha` is skipped “while none is recorded,” but the charter already records freeze SHA `7b6bc704…` and the job is live. Update the description so the register accurately states what that green tick currently claims.
