major: packages/core/src/test-command.test.ts:235 The guard only recognizes cache keys whose value starts with `turbo-`, so a Turbo result-cache key such as `v1-turbo-${{ github.sha }}` would evade the assertion even though AC-4(b) explicitly requires rejecting any `turbo-` cache key. Check for `turbo-` anywhere in the relevant cache configuration value, and add a fixture case proving a prefixed Turbo cache key fails the guard.
