major: packages/cli/src/main.ts:45 `main` retains only `cmd` from `parseArgv` and discards `rest`, `flags`, and `gateAnswers`; the handler table at line 32 consequently accepts no parsed context. Q-0091–Q-0094 would have to modify the shared frame or parse argv again, contrary to the requirement that commands be written against a frame that already parses argv. Pass the parsed result, or its command-relevant fields, through the handler contract.

major: packages/cli/src/main.ts:47 handlers are invoked without `await`, while `HANDLERS` is typed as returning only `void`. Future command handlers perform asynchronous work; a rejected handler promise would be detached, `main()` would resolve successfully, and Q-0096's required `main().catch(dieOnUnexpected)` wiring could not report it through `die`. Define handlers to return `void | Promise<void>` (or `Promise<void>`) and await their result inside `main`.
