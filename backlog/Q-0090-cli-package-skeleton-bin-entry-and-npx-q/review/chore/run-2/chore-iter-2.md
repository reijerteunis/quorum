major: packages/cli/src/fail.ts:47 `stackOf` deliberately changes the required `e.stack ?? String(e)` behavior for thrown `null`/`undefined` and symbol-valued `stack` properties: the spike raises while accessing a nullish value or concatenating a symbol, whereas this code prints a coloured value and exits through `die`. Reporting and pinning the divergence does not authorize it under AC-3 and ground rule 3. Preserve the spike’s behavior for all thrown values and add discriminating coverage.

major: packages/cli/src/package.test.ts:67 The test requires the provisional `bin` target to end in `.js`, even though the gate explicitly deferred the executable path and emit strategy to Q-0096 pending a decision entry. This would make valid Q-0096 choices such as an extensionless launcher or another output layout fail Q-0090’s suite. Remove assumptions about the target’s format and test only the `quorum` bin declaration and the requirements that Q-0090 actually owns.
