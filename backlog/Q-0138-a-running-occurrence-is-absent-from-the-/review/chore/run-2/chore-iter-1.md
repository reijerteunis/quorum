major: packages/server/src/retained.test.ts:646 AC-9 explicitly requires the real-writer arrangement behind a running daemon, but this test constructs the manifest and occurrence directory by hand. It therefore remains green if `RunHistory.allocate` stops producing a server-readable manifest or its produced shape drifts from this fixture. Drive a real `RunHistory` through allocation and prompt persistence, hold it before completion with a deterministic barrier, and query that same repository through the listening daemon.

major: apps/web/test/history-producer.test.ts:43 AC-10 requires the screen test’s wire shape to be one a real writer produced, but this test only searches TypeScript source text for literals and substrings. It neither executes the writer nor projects its output through the server wire schemas, and can pass while runtime behavior fails or the hand-built web fixture diverges in any field it does not scan. Replace this source inspection with a behavioral producer-to-wire fixture consumed by the rendering test; the added cross-package Turbo input should then be retained only if that behavioral test actually reads outside the package.

observation: GO-3’s measured performance threshold was crossed, so the requirements assign the gate/operator an obligation to open a performance successor before Q-0138 closes.
