blocker: packages/cli/src/spike-dependencies.test.ts:152 The derived key set is an inventory of files under `spike/`, not the pre-change dependency sites AC-10 requires. Broad hand-written claims such as `subject: ['src']` can cover many spike files, so deleting an individual `DISPOSITIONS` row remains green whenever another row claims the same subject. Derive an independent inventory of dependency sites and compare it one-to-one with `DISPOSITIONS`; if that cannot be done under the current requirements, return the conflict to the gate instead of substituting subject-file coverage.

major: packages/cli/src/spike-dependencies.test.ts:139 `corpus()` passes `--others --exclude-standard`, contradicting its own “tracked inventory” contract and causing an untracked local source file containing a matching read to turn the suite red. That makes the test verdict depend on checkout contents rather than the commit. Remove the untracked-file flags or explicitly resolve the requirements conflict at the gate.
