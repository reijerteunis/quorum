major: apps/web/test/source.test.ts:912 The timer guard’s home-grown binding analysis can miss an indirect poll. A function’s captured text ends at the next nested `const`/`let`/`var`/`function`, so `function reload() { const path = ...; fetchRuns(...) }` does not mark `reload` as requesting; a timer calling it passes. The fixed five propagation passes also miss longer call chains. This fails AC-11/E-2’s requirement that no fetch be reachable from a timer callback. Replace this with analysis that follows complete function bodies/call chains without an arbitrary depth, and add discriminating fixtures containing a nested binding and a chain longer than five hops.

major: apps/web/src/mission-control-status.tsx:243 `CostRegion` only knows the history roll-up and emits the absent-vendor sentence when the entire roll-up is empty. During a two-vendor run where one vendor has already completed a billed occurrence and another has not, the non-empty branch renders only the first vendor’s row; it never compares the live snapshot’s `spawn` vendor labels with the roll-up. The second vendor is therefore blank rather than receiving AC-16’s distinct “vendor in the run but absent from the roll-up” explanation. Pass the observed vendor set into this region, render the absence for every observed vendor missing from `rollup`, and test the required one-row-then-two-row transition.

nit: apps/web/src/mission-control-text.ts:74 The dry-walk copy adds “Nothing was spent either,” although AC-16 deliberately requires the absence to be explained by nothing having been recorded rather than by nothing having been spent. Keep the sentence focused on the absence of run history so it does not introduce the rejected explanation.
