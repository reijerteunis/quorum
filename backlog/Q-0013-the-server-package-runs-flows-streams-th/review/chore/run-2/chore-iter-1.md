major: packages/server/src/host.ts:333 `shutdown()` snapshots only records already marked `running`, while `start()` leaves a record marked `refused` during its awaited first pull and remains callable during or after shutdown. A concurrent start can therefore be omitted from the snapshot, become running after shutdown resolves, and keep its run lock and work alive. Introduce a closing/closed host state, reject new starts once shutdown begins, and account for starts whose first pull is still pending before shutdown resolves; add race tests for a start concurrent with shutdown and a start attempted afterward.

major: packages/server/src/refusal.ts:21 AC-5 requires the remedy-owning module to take the condition as a string and name no `core` symbol, following `dieNoProject`; this module instead imports `ProjectNotFoundError` and classifies errors with `instanceof` at line 53. The source test at `packages/server/src/package.test.ts:286` does not enforce its stated claim—it merely finds the unrelated `condition: string` interface field—so the violation passes. Move error classification outside the remedy composer so the latter accepts the condition string, and strengthen the test to reject core-symbol imports or parameters in the remedy module.
