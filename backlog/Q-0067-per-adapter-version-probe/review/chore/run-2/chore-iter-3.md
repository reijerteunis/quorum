major: packages/core/src/adapters/cli-version.test.ts:457 The AC-6 guard does not prove that the permitted reader in `packages/cli/src/adapters.ts` only renders the state. It merely pins the existing login-based exit expression and searches for state literals inside direct `die(...)` or `failSoftly(...)` calls. The command could branch on `seen.state` to select probe arguments, configuration, schema, retry behavior, or a model—and could even change an exit through an intermediate boolean—while every guard remains green. AC-6 explicitly requires the source guard to prevent any production version/state branch from selecting those behaviors, including at the rendering site. Add a structural assertion over the allowed CLI site that confines state consumption to the renderer/report fields, with a mutation demonstrating that state-dependent non-rendering behavior is rejected.
