major: packages/core/src/git/git.ts:315 An initial `rev-parse` failure is returned as `null`, so `quorum board` silently suppresses a git failure even when it occurs inside a valid work tree. AC-3 requires failed probes to produce `indeterminate (git failed)` and reserves `null` for a confirmed non-work-tree. Separate work-tree detection from shallow-state probing, classify probe failures honestly, and add a board-level regression test proving the failure renders a cannot-say line while exiting 0.

major: packages/core/src/git/git.ts:337 A configured upstream whose tracking ref no longer resolves makes `rev-list` fail and is reported as `git failed`. Section 3 and AC-3 require an unresolvable base or upstream to produce `indeterminate (missing ref)`. Verify the upstream ref before counting and add a fixture where upstream configuration remains but the remote-tracking ref is absent.
