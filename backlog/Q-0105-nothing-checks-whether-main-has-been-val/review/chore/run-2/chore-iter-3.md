major: packages/core/src/git/git.ts:70 `repositoryAt` collapses every failure of `rev-parse --resolve-git-dir` to `false`, after which `workTreeProbe` reports `outside` and `pushLag` returns silent `null`. A missing `.git` path is only one reason this command can fail; malformed gitfiles, inaccessible paths, and repositories discovered above `repoDir` can also fail this exact-path probe. The code therefore still infers “no work tree” from a failed probe, contrary to AC-3 and the documented rule that `null` means git established absence. Make the secondary classification preserve an indeterminate/failed outcome and add regression coverage for a secondary-probe failure (including a project below a refused repository); only return `outside` when absence has been conclusively established.
