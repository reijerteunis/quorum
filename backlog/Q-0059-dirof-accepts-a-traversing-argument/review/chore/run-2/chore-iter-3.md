major: packages/core/src/backlog/confine.ts:2 The module contract says the backlog store reads and writes inside its root “and nowhere else,” and lines 4–7 further claim that every path `Backlog` touches passes through this module. That is false for `read` and `list`, which deliberately open `ticket.md` without leaf confinement, as the corrected `backlog.ts` docblock and architecture document now acknowledge. This leaves the same contradictory source-level security contract that iteration 3 intended to remove. Narrow this docblock to the four guarded methods and explicitly preserve the `read`/`list` exception.
