# Code review — Q-0127, chore run 2, iteration 2

major: apps/web/src/app.tsx:157 `TicketPage` is reused without a `key` when `ticketId` changes, while its state is only cleared later by the effect at `ticket-page.tsx:271`. The first render after navigation therefore still commits the previous ticket’s detail, selected file, and run log under the new URL until the effect runs, contrary to AC-9’s requirement that a different id render nothing from the previous ticket. The existing test flushes effects inside `act`, so it cannot observe this intermediate stale commit. Remount the page by ticket id (for example, by keying it here), or synchronously gate rendered state by the id that produced it, and add a test that inspects the navigation commit before the replacement request settles.

major: apps/web/src/daemon-client.ts:129 AC-8 requires each of `fetchTicket` and `fetchTicketFile` to be driven through good, 404, 422, non-JSON, and wrong-shape responses, but `daemon-client.test.ts` imports and exercises neither function. The component suite covers the detail request’s broad UI states and one file refusal, but does not verify both new schemas, both generated paths, or either function’s non-JSON behavior. Add the stipulated table-driven client tests for both functions, including distinct 404 and 422 refusals and validation failures.
