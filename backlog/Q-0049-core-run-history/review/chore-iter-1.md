major: packages/core/src/run-history/writer.ts:241 A `.quorum/runs` path that is a file throws the raw `mkdirSync` `EEXIST` error, but AC-2 explicitly requires this fixture to produce the named `FlowError` “could not create” refusal. Catch and translate failure to create the runs root while preserving the required refusal ordering and message.

major: packages/core/src/run-history/writer.ts:343 `terminal()` treats an existing `output.txt` directory as satisfying the artifact guarantee because `existsSync` does not distinguish files from directories. AC-5 explicitly requires this case to warn with the output path while still persisting the terminal occurrence. Verify that the existing path is a regular file, and otherwise attempt the write so the injected warning channel reports the failure.

major: packages/core/src/run-history/writer.ts:356 `persist()` accepts only `string` and writes it directly, narrowing the ported `String(text)` behavior required by AC-6 and the spike. JavaScript callers or future internal callers passing another string-convertible value now get different runtime behavior. Preserve the ported conversion by accepting an appropriate non-`any` input type and passing `String(text)` to `writeFileSync`.
