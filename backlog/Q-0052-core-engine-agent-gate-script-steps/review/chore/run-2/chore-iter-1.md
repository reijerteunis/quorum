major: packages/core/src/engine/steps.ts:134 `resolveModel` inherits `role.meta.model` when `role.meta.adapter` is absent. Thus a role containing `model: opus` but no adapter can pass `opus` to a step resolved to `codex`, contradicting AC-4(a), which permits inheritance only when the role adapter equals the resolved adapter. Return the role model only on explicit adapter equality, and update the test at `packages/core/src/engine/steps.test.ts:30` that currently endorses the unsafe fallback.
