major: spike/src/lint.js:44 `flattenSteps(flow.steps)` does not descend into a `fan_out` step's `step` template, so an `input.diff` there bypasses the new static rule and can survive `harness lint` until runtime. AC-10 applies to every `input.diff`; include fan-out templates in lint traversal and add a malformed-template test.

major: spike/src/git.js:64 A failure of `git rev-parse --is-shallow-repository` is silently converted to `false`. If the subsequent ancestry command exits 1 in a shallow repository, the implementation reports `not contained` even though it could not determine whether shallow-history asymmetry applies. This violates AC-3's rule that execution failures produce `indeterminate`; preserve probe failure as a third state and propagate a concise reason instead of defaulting to non-shallow.

major: docs/DECISIONS.md:475 `git log -1 --format='%p' ebf1c6e` proves only that `998f397` is the merge commit's first parent; it does not prove that `ebf1c6e` was the branch's next ref movement after run 10, that the branch had not moved or been reset in the intervening period, or that it pointed at `998f397` throughout the run. The entry therefore does not establish the run-time branch head by the durable timestamp route AC-12 requires. Add clone-surviving evidence that independently establishes those assertions, or narrow the conclusion to what the recorded commands actually prove.
