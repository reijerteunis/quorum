major: spike/src/engine.js:820 A deferred range only names its producing step when the unresolved ref is exactly the deferred endpoint. If the other endpoint is missing, as in E16(a), the message omits the step and branch that caused preflight to defer the range, despite AC-9 requiring deferred empty, missing, and indeterminate failures to additionally name the expected producer. Preserve the distinction about which endpoint is missing, but always include the deferred producer and the ref it was expected to create.
