major: spike/src/engine.js:119 The run-level preflight examines only each outer step's `input.diff`; unlike the new lint traversal, it never examines a `fan_out` step template's `step.input.diff`. A valid non-empty template range is consequently materialised separately by every expanded adapter through `buildPrompt`, violating AC-11's once-per-distinct-range guarantee and allowing panel members to receive evidence resolved at different times. Traverse fan-out template diff sites during preflight and cache or defer each range before expansion.

major: spike/test/q0035-empty-range.js:355 The AC-8 adapter-boundary table omits the required indeterminate pre-existing-ref case, and the malformed case is checked only through `lintFlow` rather than by counting adapter occurrences at the runtime boundary as AC-8 requires. Add run-level fixtures for an indeterminate range and a malformed range, asserting zero recorded adapter invocations.

major: spike/test/q0035-empty-range.js:413 The sole deferred-range runtime scenario covers only an empty contained range. AC-9 explicitly requires missing and indeterminate deferred ranges to receive the same evidence and producer/consumer invocation accounting. Add deferred missing-ref and indeterminate fixtures that assert the producer ran, the consumer did not, and the failure names the producing step while satisfying AC-1 through AC-6.
