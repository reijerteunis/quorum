major: packages/core/src/engine/composite.ts:255 The base-conflict path throws after allocating an integrate occurrence but bypasses `persistArtifact` and `terminalOccurrence`. This violates AC-12’s requirement that `output.txt` is always persisted, empty included, and that conflict outcomes close the occurrence as failed. Persist the empty output and close the occurrence with category `integrate` before throwing, while preserving AC-8’s required notes/log ordering.

major: packages/core/src/engine/composite.ts:319 The report-routing predicate calls `target.includes(...)` on the original flow-authored value even though AC-13 requires every `writesOf(step)` entry to be coerced at its call site. A numeric YAML path reaches this line and throws despite being stringified for interpolation one line earlier. Coerce once with `String(target)` and use that string for both interpolation and the `report` check; add a non-string write-path test.
