# Review

major: apps/web/src/ticket-page.tsx:519 The `holds` predicate treats either missing refresh result as sufficient to preserve the confirmation. Consequently, if the refreshed flow listing reports that the selected flow was removed or refused while the ticket read fails, `stage === null` keeps the stale confirmation actionable; likewise, a reported stage change is ignored when the flow read fails. This violates AC-9/E-6 because available daemon evidence can already disprove the premise. Preserve the last reported stage and flow listing independently, or use tri-state logic that withdraws whenever either new authoritative report conclusively makes the selected flow ineligible while retaining the confirmation only when missing data genuinely prevents that conclusion. Add tests for both mixed outcomes: changed flows plus failed ticket read, and changed stage plus failed flow read.

observation: The supplied patch omitted `docs/05-design-prompt.md`, `packages/server/src/http.ts`, `packages/server/src/package.test.ts`, `packages/shared/src/docs.test.ts`, `packages/shared/src/wire.test.ts`, and `packages/shared/src/wire.ts`; I inspected those files directly from `harness/Q-0130/implement`.
