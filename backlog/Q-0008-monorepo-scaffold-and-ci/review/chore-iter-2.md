major: package.json:8 The project declares support for every Node version from 22.0 onward, but the committed ESLint 10.9.0 dependency requires Node 22.13 or newer and Vite 8.2.2 requires Node 22.12 or newer. Contributors on Node 22.0–22.12 therefore satisfy the repository’s advertised engine range while using an unsupported toolchain. Resolve the requirements conflict, then either raise the declared minimum to `>=22.13.0` or select maintained dependencies that support the full declared range.
