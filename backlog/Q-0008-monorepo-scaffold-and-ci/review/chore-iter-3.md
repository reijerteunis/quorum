major: package.json:8 The repository advertises support for every Node version from 22.0 onward, but the locked ESLint toolchain requires Node 22.13.0 or newer and Vite requires at least 22.12.0. Contributors on Node 22.0–22.12 therefore receive a machine-readable compatibility promise the scaffold cannot honor. Amend the requirement and set `engines.node` to `>=22.13.0`, or use maintained dependencies that genuinely support the full declared range.
