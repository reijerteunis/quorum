major: docs/DECISIONS.md:670 AC-2 requires a dated behaviour-preservation decision, but this diff contains only the routing decision; the policy in the charter is not a substitute for the required durable decision record. Add the drafted behaviour-preservation entry with Decision, Alternatives considered, and Why before merging.

major: backlog/Q-0009-port-the-spike-to-packages-core/ticket.md:28 AC-8 explicitly requires Q-0009’s body and all fourteen child bodies to be reconciled, but none is changed: the parent still assigns cutover to Q-0009, and the children still lack the required routing citation, dependencies, inherited invariants, and non-goals. Update the ticket bodies through an authorized backlog-writing step; duplicating the material in `harness/port-charter.md` does not satisfy the criterion.

major: harness/port-charter.md:9 The requirement explicitly places the charter in `backlog/Q-0009-port-the-spike-to-packages-core/`, while the implementation unilaterally relocates it to `harness/`. The delivery limitation explains why this implementer could not satisfy the criterion, but does not authorize changing it. Obtain an accepted requirement amendment or arrange an authorized backlog commit, while retaining any harness pointer needed to expose the charter to child agents.

major: .github/scripts/port-freeze-guard.sh:87 When no freeze SHA exists, AC-6 requires the guard to be inert and not produce a successful check, but the script continues to `exit 0`, so GitHub renders a green job despite the warning. Once a SHA is recorded, line 91 likewise performs no verification at all. Make the CI result represent skipped/inert state without a green success, and enforce the recorded SHA rather than merely printing it.

major: .github/scripts/port-freeze-guard.sh:71 Any commit message beginning with `Port-freeze-exemption:` bypasses the freeze, even when it omits the required ticket ID and reason or names another ticket. Validate the complete `<current-ticket> <non-empty reason>` marker before honoring it so an accidental or unrelated trailer cannot disable the guard.
