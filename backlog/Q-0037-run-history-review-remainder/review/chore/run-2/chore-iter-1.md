major: harness/port-charter.md:272 `freeze-sha` remains at `a6e529a…` even though this change modifies `spike/src`, so the freeze guard will fail after merge and AC-12’s required re-recording is incomplete. Re-record the SHA through the integration mechanism at the post-merge tip and run the freeze-SHA guard against that tip before closing the ticket.

major: spike/bin/harness.js:449 The `unrecognised-annotation` outcome covers both an absent annotation and a present but unsupported value, yet the notice always claims there is “no x-quorum-contract annotation.” A schema with `x-quorum-contract: unknown-v1` therefore receives factually incorrect guidance, contrary to AC-10’s honest/inapplicable notice requirement. Use wording valid for both cases (such as “no recognised x-quorum-contract annotation”), or distinguish the outcomes, and add a literal assertion for the existing unknown-value fixture.
