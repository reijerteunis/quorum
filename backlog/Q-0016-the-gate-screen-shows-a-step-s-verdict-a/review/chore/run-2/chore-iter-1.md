blocker: apps/web/src/gate-screen.tsx:300 `load()` clears the in-flight guard while the existing POST is still unresolved, so pressing Refresh re-enables the controls and permits a second, potentially different answer to race the first; the older request also clears `sending` unconditionally at line 320 and can permit further submissions while the newer request remains pending. This violates AC-11’s requirement that every answer control remain inert while an answer request is in flight and can make `abort` win after the user first chose `advance`. Keep the submission guard held until the outstanding POST settles, and do not expose enabled answer controls during a refresh while it remains outstanding.

major: apps/web/src/app.tsx:92 The shell creates and connects a `RunConnection` for every route containing `:handle`, including `/runs/:handle/gate` at lines 103–126. Consequently, visiting the gate screen does open a WebSocket, contrary to erratum E-2/GO-4’s ratified requirement that this screen hold no socket and obtain its state only through explicit reads. Exclude the gate route from live-connection setup while retaining the connection for the run screens that require it.

major: apps/web/src/gate-screen.tsx:123 The refused-run state explicitly says that the route carries no refusal reason, but AC-10 requires a refused start to report the daemon’s own condition. The implementation report acknowledges this weaker behavior, yet no erratum relaxes the criterion. Carry the refusal condition through the run response and render it, or obtain an explicit requirements ruling before claiming AC-10 is satisfied.
