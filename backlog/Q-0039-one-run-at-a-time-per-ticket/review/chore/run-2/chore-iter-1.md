major: packages/core/src/run-history/writer.ts:359 The ownership check and deletion are separate filesystem operations: after `readLockRecord` confirms this run's token, a human can remove the file and a successor can create a new lock before line 372 unlinks the pathname. The first run would then delete the successor's lock, violating AC-5's explicit ownership guarantee and reopening concurrent execution. Use a lock representation/release protocol whose atomic filesystem operation cannot remove a replacement owner (for example, a directory/token-sentinel design), or revise the approved contract before implementation.

major: packages/core/src/run-history/writer.ts:339 Once `openSync(..., 'wx')` succeeds, a failure while writing or closing is caught as a failed acquisition, but the newly created empty or partial file is never removed and no `RunLock` handle is returned. A recoverable write/close error therefore permanently blocks the ticket with a damaged lock until manual intervention, even though no run owns it. Track whether this invocation created the file and best-effort remove that exact acquisition on initialization failure, while preserving the original error; add tests for both write and close failures.
