major: packages/cli/src/build.test.ts:330 AC-8 snapshots only path names and defines `written` as paths absent from that snapshot. A build that overwrites an existing file outside `dist/` is therefore omitted from `written`, and output written outside the emitting package directories is never examined at all. This can let undeclared build writes pass despite AC-8 requiring enumeration of every path written. Instrument or isolate the real build so the test compares the complete filesystem write set against `outputs`, including overwritten paths and writes outside package roots.

major: packages/cli/src/build.test.ts:562 The AC-23 present/absent test merely lists files ending in `.test.ts`; it neither applies Vitest’s configured include/exclude patterns nor asks Vitest what it collects. Consequently an emitted `dist/x.test.js` can never enter either side of this comparison even if the `dist` exclusion is removed, so the required collected-test-set equivalence is not established. Derive both sets through Vitest’s actual resolved collection behavior, or faithfully apply both configured includes and excludes to all candidate files before comparing them.
