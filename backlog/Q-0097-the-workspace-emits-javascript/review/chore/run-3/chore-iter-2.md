major: packages/cli/src/build.test.ts:182 The AC-8 audit excludes `.git`, `.harness`, and `.quorum`, although AC-8 requires enumerating every path the real build writes and exempts only Turbo cache metadata and logs. A changed build script could write into or delete content under any of these directories while the exact-output check remains green; the test explicitly acknowledges this blind spot at lines 177–180, making it an unchosen weakening of the criterion. Replace the broad pruning with an observation strategy that distinguishes build writes from concurrent activity—such as instrumenting the build or running the audited build in an isolated workspace—while retaining the separately required real-workspace build proof.
