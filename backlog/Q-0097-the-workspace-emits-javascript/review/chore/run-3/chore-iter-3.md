# Review — Q-0097, chore run 3, iteration 3

major: packages/cli/src/build.test.ts:303 The new isolated-workspace audit contradicts binding erratum E-1, which explicitly rules that AC-8 uses the five-entry `UNAUDITED` exemption set and expressly rejects splitting the proof into an isolated exactness audit plus a real-workspace build proof. Lines 303–365 implement that rejected alternative, while line 191 replaces the ruled five-name register with `INSTALLED = ['node_modules']`. This is not an implementation detail the developer may choose after the human ruling; it changes the accepted coverage/cost tradeoff and adds substantial unrequested machinery. Restore the E-1 audit shape, or obtain a new human-authored erratum that explicitly supersedes E-1 before retaining this strategy.
