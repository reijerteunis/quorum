major: packages/cli/src/validate.ts:56 `validate` deliberately does not call `loadProject()`, while binding AC-4 explicitly requires both commands to call it first and handle `ProjectNotFoundError`. The spike evidence makes AC-4 appear incorrect, but the implementer cannot unilaterally replace a binding criterion. Add a gate erratum limiting AC-4 to `lint`, or change the implementation and tests to satisfy the current criterion.

major: packages/cli/src/lint.ts:60 `lint` reads and applies `flags.project`, contrary to AC-2’s explicit statement that it reads neither `rest` nor `flags`. Preserving the spike’s existing `--project` behavior is well-supported, but no binding erratum currently authorizes this divergence. Correct AC-2 at the gate or change the handler to meet it; do not merge an undocumented default choice.
