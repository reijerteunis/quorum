major: spike/src/git.js:45 Using `%(refname:short)` does not reliably return the actual branch name: when another ref such as a tag shares that name, Git may emit a disambiguated value such as `heads/foo`. The subsequent `branches.has(branch)` then treats the real `refs/heads/foo` branch as unresolved and leaves its ticket unannotated, violating the requirement to annotate every ticket whose branch resolves. List branch names relative to `refs/heads/` without Git’s ambiguity-dependent shortening, and add a fixture containing a same-named tag.
