major: packages/shared/src/docs.test.ts:889 AC-14 requires the documented nullable token shape to be pinned against `ProbeResult`, but this test only matches three phrases within `docs/03-adapter-contract.md`. It therefore stays green if `ProbeResult.tokens` is changed back to `number` while the documentation remains unchanged. Read `packages/core/src/adapters/adapters.ts`—already a declared test input—and assert the documented `cost_usd`/`tokens` nullability against the actual `ProbeResult` declaration rather than only checking self-consistency within the prose.
