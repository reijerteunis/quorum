blocker: backlog/Q-0101-the-mock-end-to-end-s-gate-rollback-and-/dev/chore/run-2/implement-iter-1.md:321 GO-5 requires repeated sweeps at this branch’s actual merge base and on the implement branch, including failure counts and failing files. The report substitutes measurements from different commits and admits the sole branch failure’s files were not captured. Run matched samples at `edcc7ad` and the implement tip with full output capture, then report both rates and failing files.

major: packages/cli/src/failure-paths.test.ts:72 R-6 requires a module-scope refusal whenever `sh` is unavailable, but this only checks whether the platform is Windows. A Unix-like environment with no usable `sh` proceeds and fails later for an unrelated command error. Probe `sh` availability at module scope and issue the required named refusal when it cannot execute.

major: packages/cli/src/failure-paths.test.ts:676 AC-7(b1)(b) requires the output to name the two specific conflicting branches, but `/cannot sync .* with /` passes for any two—or even empty/misleading—subjects. Assert both `main` and `harness/T-0001/integration` in the diagnostic so incorrect branch attribution is caught.
