blocker: packages/core/src/lint/lint.test.ts:331 The test accepts trailing whitespace on a ticket-prefixed endpoint even though AC-4’s test clause explicitly requires trailing whitespace to be refused. The implementation report correctly identifies the conflicting authorities, but reporting the conflict does not satisfy both requirements or close the prior blocker. Obtain an accepted requirements erratum choosing the preserved spike grammar or authorizing a narrower grammar, then align this test and implementation with that decision before approval.
