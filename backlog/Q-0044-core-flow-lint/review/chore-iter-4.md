major: packages/core/src/lint/lint.ts:259 `FlowRecord` is exported, and `FlowFileReport` and `DirectoryReport` are likewise exported at lines 367 and 377, expanding the TypeScript module surface beyond AC-1’s exact six names even though the runtime-only `Object.keys` test cannot detect type exports. Keep these implementation types private or otherwise remove the extra exports, and add a type-level/source assertion that prevents additional exported declarations.
