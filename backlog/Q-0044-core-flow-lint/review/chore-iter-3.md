blocker: packages/core/src/lint/lint.test.ts:331 The test explicitly accepts trailing whitespace on a ticket-prefixed endpoint, while AC-4 explicitly lists trailing whitespace among the forms that must be refused. The implementation report correctly identifies the contradiction, but documenting it does not satisfy the current acceptance criterion. Obtain and commit the proposed requirements erratum, or change the implementation and tests to reject the specified trailing-whitespace case before approval.
