major: packages/core/src/fanout/fanout.test.ts:499 The snapshot helper removes every path beneath `.harness/`, while AC-11 permits changes only beneath `.harness/worktrees/` and `.git/`. A regression writing elsewhere under `.harness/` would therefore pass this safety test. Narrow the exclusion to `.harness/worktrees/` so other `.harness` changes remain visible in the before/after comparison.
