major: packages/core/src/turbo-inputs.test.ts:684 Clause C only prohibits `fileURLToPath` outside the route modules and scans calls under their original helper names. A test can therefore import `repoFile` under an alias and pass a computed argument, or derive the repository root through `process.cwd()` and read a computed path; neither clause B nor C will see the resulting out-of-package read. This contradicts AC-7’s requirement that a new read cannot bypass the manifest unnoticed, and the report explicitly acknowledges the alternative-root hole. Make the escape-route check account for import aliases and fail closed on repository-root derivations/read APIs rather than recognizing only `fileURLToPath`, with independent fixtures demonstrating both bypasses fail.
