major: packages/core/src/turbo-inputs.test.ts:265 The AC-7 scanner only recognizes existing repository paths written as single- or double-quoted literals; template literals, computed arguments, and direct repository-root reads without such a literal bypass it. Therefore a new `repoFile(`docs/${name}.md`)` or equivalent `fs.readFileSync(path.join(repoRoot, ...))` can add an undeclared out-of-package read while this guard remains green, contradicting AC-7’s requirement that new helper calls cannot bypass the manifest and that named corpus helpers are the only route out of a package. Make the escape-route clause inspect the relevant call expressions and fail on non-literal or unaudited repository reads, with an independent fixture proving that clause fails.
