major: packages/cli/src/open.ts:307 The `createDaemon` catch converts every `Error` into a one-line expected refusal, although AC-4 authorizes the command to catch only the missing-bundle condition (plus AC-5’s explicit `EADDRINUSE` case). Other failures such as permission errors or internal daemon defects therefore lose their stack and bypass `dieOnUnexpected`, hiding actionable diagnostic context. Give the bundle refusal a distinguishable type/code and catch only that and `EADDRINUSE`; rethrow every other error unchanged.
