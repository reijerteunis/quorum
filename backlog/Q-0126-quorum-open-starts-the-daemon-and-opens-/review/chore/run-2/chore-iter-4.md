major: packages/cli/src/open.ts:279 The positional-argument guard checks only `rest`, but the generic parser consumes the token after any flag as that flag’s value. Consequently, `quorum open --no-open my-project` produces `flags['no-open'] === 'my-project'`, leaves `rest` empty, and starts the daemon for the current/default project while silently discarding the apparent positional argument—the exact failure AC-2 requires preventing. Require `--no-open` to have the boolean value `true` (and reject any consumed value with the usage message), with an argv-level regression test covering this ordering.

observation: Focused Vitest execution could not start in the read-only review environment because Vite attempted to create a file under `packages/core/node_modules/.vite-temp` and received `EPERM`.
