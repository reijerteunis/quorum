major: packages/cli/src/binary-name.test.ts:121 The scanner decodes only `\n`, `\t`, and `\r`; every other escape is reduced to the character immediately after the backslash. Consequently, a printable literal such as `'harness\x20init'` or `'harness\u0020init'` is interpreted as whitespace-free (`harnessx20init` / `harnessu0020init`) and filtered out even though the runtime prints `harness init`. This lets a user-facing sentence name the wrong binary while AC-4 remains green, contradicting the guard’s purpose and the claim at lines 76–77 that other escapes decode to the character they escape. Parse literals with TypeScript-aware tooling or correctly decode JavaScript escape forms, and add discriminating tests for hexadecimal and Unicode whitespace escapes.
