major: packages/core/src/adapters/claude.ts:134 The port replaces a non-string `env.result` with the entire stdout and discards non-string session and usage values, whereas the spike and AC-6/AC-7 specify `env.result ?? stdout`, `env.session_id ?? null`, and direct vendor-field propagation. The implement report acknowledges the same narrowing in both adapters, but charter §2 requires divergences to stop the ticket rather than ship as TypeScript cleanup. Preserve the spike semantics with narrowly justified typing/casts, or obtain explicit authority and update the contract and requirements before implementing the changed behavior.
