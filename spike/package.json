{
  "name": "quorum (spike)",
  "version": "0.0.1",
  "description": "Spike: stage-chained, multi-vendor SDLC flows on subscription-authed CLI agents, backlog as files in git.",
  "type": "module",
  "bin": {
    "harness": "bin/harness.js"
  },
  "scripts": {
    "test": "node test/run.js",
    "lint": "node bin/harness.js lint"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "ajv": "^8.20.0",
    "ajv-formats": "^3.0.1",
    "yaml": "^2.5.0"
  }
}
