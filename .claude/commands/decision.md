Append a decision to docs/DECISIONS.md about: $ARGUMENTS

Format exactly: `## <title> — <YYYY-MM-DD>` then **Decision:**, **Alternatives considered:**, **Why:**. If this reverses or refines an earlier entry, name it. If it introduces a term, add it to docs/GLOSSARY.md in the same change. Show me the entry before writing it.
