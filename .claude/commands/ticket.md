Create a new ticket in backlog/ for: $ARGUMENTS

Use the next free `Q-nnnn` id (scan backlog/). Folder name `Q-nnnn-<slug>`. Write `ticket.md` with frontmatter (id, title, stage: draft, owner: ruud, branch: harness/Q-nnnn/integration, priority, created, iterations: {}, history: []) and a one-paragraph intent body. Say which milestone in docs/06-development-plan.md it belongs to. Do not write requirements — that is the requirements flow's job.
