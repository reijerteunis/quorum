# Engineering rules (canonical — compiled into CLAUDE.md / AGENTS.md later)

- TypeScript strict; no `any`.
- Conventional commits.
- Every behaviour change ships with a test.
- No new dependency without a one-line justification in the solution document.
