{
  "name": "@quorum/cli",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "bin": {
    "quorum": "./dist/quorum.js"
  },
  "files": [
    "dist"
  ],
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "build": "rm -rf dist && tsc -p tsconfig.build.json && chmod +x dist/quorum.js",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@quorum/core": "workspace:*",
    "@quorum/shared": "workspace:*"
  }
}
